export enum AdminRoutes {
    Dashboard = 'dashboard',
    Events = 'events',
    Settings = 'settings',
    Elements = 'elements'
}

export enum ElementRoutes {
    Alert = 'alert',
    Modal = 'modal'
}

export enum SettingRoutes {
    Profile = 'profile',
    Users = 'users',
}